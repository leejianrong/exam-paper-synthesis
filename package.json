{
  "name": "exam-paper-synthesis-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Root-level Playwright end-to-end tests (isolated from web/). See ADR-0008.",
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
